node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Private dev tooling — the autoresearch loop that improves the detector.
# Stays local: publishing the eval corpus + internals would let anyone game the detector.
eval/
